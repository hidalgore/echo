# ECHO_WEBSITE_V58_HOST_FIRST_HOMEPAGE_LOCK — DEPRECATED

**Status:** **DEPRECATED in v59.2.** Superseded by `ECHO_WEBSITE_V59_2_BALANCED_HOMEPAGE_LOCK.md`.

---

## What changed

v58 positioned the public homepage as **host-first**: the H1, the primary CTA, and the section arc all spoke primarily to hosts. v59.2 explicitly rebalances the homepage to serve **attendees and hosts equally**, while introducing **"ECHO Access"** as the formal product line.

The host-first positioning is no longer canonical. Do not implement against the v58 doc.

## Why it was deprecated

Direct founder direction in the v59.2 session: rebalance the homepage to serve both audiences, make it informative on **who and what ECHO Access is**, and elevate to an editorial / "ultra elite" presentation.

## What is locked now (v59.2)

See `ECHO_WEBSITE_V59_2_BALANCED_HOMEPAGE_LOCK.md`. Headline locked decisions:

- **Hero:** Unified, one H1, two equal-weight CTAs ("Explore Events" / "Become a Host").
- **H1:** "ECHO Access is the premium platform for events that feel as good as they look."
- **Brand:** "ECHO Access" is the product line throughout the public site.
- **Body arc:** Alternating attendee/host bands, capped by a shared trust pillars section and a closing unified CTA.
- **Intro section:** Side-by-side editorial prose + ECHO Access Pass mockup.

## What carries forward unchanged

- "Reserve Access" not "Buy Ticket"
- Age verification BEFORE payment
- ECHO Circle: leader pays first, 1-hour timer
- NFC-primary entry, QR fallback
- Donation transparency: cause + amount + routing visible before confirm
- Calm Picked for You (6 allowed reason labels)
- No emoji, no HTML `<form>`, `brand.xxx` inside `StyleSheet.create()`
- Profile screens protected
